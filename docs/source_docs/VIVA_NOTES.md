# Viva Notes

Important defense points:

- The model uses cluster-derived proxy labels, not externally audited ground truth.
- Leakage-sensitive columns are blocked from classifier inputs.
- The final risk class is conservative because guardrails can increase risk severity.
- The blockchain component is simulated and used for auditability demonstration.
- The restructuring did not change model logic, trained weights, or risk rules.
